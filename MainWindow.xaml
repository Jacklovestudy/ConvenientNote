<Window x:Class="ConvenientNote.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:prism="http://prismlibrary.com/"
        mc:Ignorable="d"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title, FallbackValue=Convenient Note}"
        Height="720"
        Width="1180"
        MinHeight="620"
        MinWidth="960"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="DateButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="Margin" Value="2,0" />
            <Setter Property="MinWidth" Value="56" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="Chrome"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Chrome" Property="Background" Value="#EEF2F6" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                    <Setter Property="Background" Value="#2563EB" />
                    <Setter Property="Foreground" Value="White" />
                </DataTrigger>
                <DataTrigger Binding="{Binding IsToday}" Value="True">
                    <Setter Property="FontWeight" Value="SemiBold" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="#F4F6F8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="236" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="#FFFFFF"
                BorderBrush="#DFE4EA"
                BorderThickness="0,0,1,0"
                Padding="18">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel>
                    <TextBlock Text="Convenient Note"
                               Foreground="#111827"
                               FontSize="18"
                               FontWeight="SemiBold" />
                    <TextBlock Text="{Binding WorkspaceName}"
                               Margin="0,4,0,0"
                               Foreground="#667085"
                               FontSize="12" />
                </StackPanel>

                <TextBox Grid.Row="1"
                         Margin="0,20,0,12"
                         materialDesign:HintAssist.Hint="搜索"
                         BorderBrush="#D0D5DD"
                         BorderThickness="1"
                         Padding="10,7" />

                <ListBox Grid.Row="2"
                         ItemsSource="{Binding NavigationItems}"
                         BorderThickness="0"
                         Background="Transparent"
                         SelectedIndex="0">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="{x:Type ListBoxItem}">
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0,2" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="10,8" CornerRadius="6">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}"
                                               Foreground="#1F2937"
                                               FontSize="14"
                                               FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding Description}"
                                               Margin="0,2,0,0"
                                               Foreground="#667085"
                                               FontSize="11" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <StackPanel Grid.Row="3"
                            Orientation="Horizontal"
                            HorizontalAlignment="Stretch"
                            Margin="0,14,0,0">
                    <Button Content="已达成"
                            Margin="0,0,8,0"
                            Padding="8,6" />
                    <Button Content="回收站"
                            Padding="8,6" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="86" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="#FFFFFF"
                    BorderBrush="#DFE4EA"
                    BorderThickness="0,0,0,1"
                    Padding="20,14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Content="‹"
                                Width="32"
                                Height="32"
                                Command="{Binding PreviousWeekCommand}" />
                        <ItemsControl ItemsSource="{Binding DateTabs}" Margin="8,0">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Style="{StaticResource DateButtonStyle}"
                                            Command="{Binding DataContext.SelectDateCommand, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"
                                            CommandParameter="{Binding}">
                                        <StackPanel>
                                            <TextBlock Text="{Binding WeekLabel}"
                                                       HorizontalAlignment="Center"
                                                       FontSize="12" />
                                            <TextBlock Text="{Binding DayLabel}"
                                                       HorizontalAlignment="Center"
                                                       FontSize="15"
                                                       FontWeight="SemiBold" />
                                        </StackPanel>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <Button Content="›"
                                Width="32"
                                Height="32"
                                Command="{Binding NextWeekCommand}" />
                        <Button Content="今天"
                                Margin="10,0,0,0"
                                Padding="12,6"
                                Command="{Binding SelectTodayCommand}" />
                        <TextBlock Text="{Binding CurrentDateTitle}"
                                   VerticalAlignment="Center"
                                   Margin="14,0,0,0"
                                   Foreground="#344054"
                                   FontSize="14"
                                   FontWeight="SemiBold" />
                    </StackPanel>

                    <StackPanel Grid.Column="2"
                                Orientation="Horizontal"
                                VerticalAlignment="Center">
                        <TextBox Width="260"
                                 materialDesign:HintAssist.Hint="在此添加待办，回车创建"
                                 Text="{Binding QuickAddTitle, UpdateSourceTrigger=PropertyChanged}"
                                 Padding="10,7">
                            <TextBox.InputBindings>
                                <KeyBinding Key="Enter" Command="{Binding AddTodoCommand}" />
                            </TextBox.InputBindings>
                        </TextBox>
                        <Button Content="新增待办"
                                Margin="10,0,0,0"
                                Padding="14,7"
                                Command="{Binding AddTodoCommand}" />
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Row="1"
                    Margin="20"
                    Background="#EEF2F6"
                    BorderBrush="#D0D5DD"
                    BorderThickness="1"
                    CornerRadius="8"
                    ClipToBounds="True">
                <Grid>
                    <ItemsControl ItemsSource="{Binding TodoItems}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas Background="#F8FAFC" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="{x:Type ContentPresenter}">
                                <Setter Property="Canvas.Left" Value="{Binding X}" />
                                <Setter Property="Canvas.Top" Value="{Binding Y}" />
                                <Setter Property="Panel.ZIndex" Value="{Binding ZIndex}" />
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="{Binding Width}"
                                        Height="{Binding Height}"
                                        Background="{Binding Color}"
                                        BorderBrush="#D6B84F"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Padding="12"
                                        Cursor="SizeAll"
                                        PreviewMouseLeftButtonDown="TodoCard_PreviewMouseLeftButtonDown"
                                        PreviewMouseMove="TodoCard_PreviewMouseMove"
                                        PreviewMouseLeftButtonUp="TodoCard_PreviewMouseLeftButtonUp">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="*" />
                                        </Grid.RowDefinitions>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <CheckBox IsChecked="{Binding IsCompleted, Mode=TwoWay}"
                                                      VerticalAlignment="Center"
                                                      Margin="0,0,8,0" />
                                            <TextBox Grid.Column="1"
                                                     Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                                                     BorderThickness="0"
                                                     Background="Transparent"
                                                     FontSize="15"
                                                     FontWeight="SemiBold"
                                                     Foreground="#111827"
                                                     LostFocus="TodoTitle_LostFocus" />
                                        </Grid>

                                        <TextBox Grid.Row="1"
                                                 Text="{Binding Content, UpdateSourceTrigger=PropertyChanged}"
                                                 materialDesign:HintAssist.Hint="补充说明"
                                                 AcceptsReturn="True"
                                                 TextWrapping="Wrap"
                                                 BorderThickness="0"
                                                 Background="Transparent"
                                                 Foreground="#475467"
                                                 Margin="24,8,0,0"
                                                 LostFocus="TodoContent_LostFocus" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <TextBlock Text="画布区"
                               HorizontalAlignment="Right"
                               VerticalAlignment="Bottom"
                               Margin="0,0,18,14"
                               Foreground="#98A2B3"
                               FontSize="12" />
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
