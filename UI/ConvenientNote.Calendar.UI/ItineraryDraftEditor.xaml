<UserControl x:Class="ConvenientNote.Views.ItineraryDraftEditor"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
 <Grid>
  <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
  <StackPanel><TextBlock Text="标题" Margin="0,0,0,4"/><TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" MaxLength="80" Padding="7" AutomationProperties.Name="编辑日程标题"/></StackPanel>
  <StackPanel Grid.Row="1" Margin="0,12">
   <CheckBox Content="待安排（不指定日期）" IsChecked="{Binding IsUnscheduled}" Margin="0,0,0,8"><CheckBox.Style><Style TargetType="CheckBox"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding IsChecklist}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></CheckBox.Style></CheckBox>
   <CheckBox Content="全天（结束日期包含当天）" IsChecked="{Binding IsAllDay}"><CheckBox.Style><Style TargetType="CheckBox"><Style.Triggers><DataTrigger Binding="{Binding IsUnscheduled}" Value="True"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style></CheckBox.Style></CheckBox>
  </StackPanel>
  <Grid Grid.Row="2"><Grid.Style><Style TargetType="Grid"><Style.Triggers><DataTrigger Binding="{Binding IsUnscheduled}" Value="True"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style></Grid.Style><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="12"/><ColumnDefinition/></Grid.ColumnDefinitions>
   <StackPanel><TextBlock Text="开始日期"/><DatePicker SelectedDate="{Binding StartDate}" Language="zh-CN" AutomationProperties.Name="开始日期"/><TextBox Text="{Binding StartTime, UpdateSourceTrigger=PropertyChanged}" Margin="0,6" Padding="6" AutomationProperties.Name="开始时间 HH:mm" ToolTip="定时日程的开始时间 HH:mm"><TextBox.Style><Style TargetType="TextBox"><Style.Triggers><DataTrigger Binding="{Binding IsAllDay}" Value="True"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style></TextBox.Style></TextBox></StackPanel>
   <StackPanel Grid.Column="2"><TextBlock Text="结束日期"/><DatePicker SelectedDate="{Binding EndDate}" Language="zh-CN" AutomationProperties.Name="结束日期"/><TextBox Text="{Binding EndTime, UpdateSourceTrigger=PropertyChanged}" Margin="0,6" Padding="6" AutomationProperties.Name="结束时间 HH:mm" ToolTip="定时日程的结束时间 HH:mm"><TextBox.Style><Style TargetType="TextBox"><Style.Triggers><DataTrigger Binding="{Binding IsAllDay}" Value="True"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style></TextBox.Style></TextBox></StackPanel>
  </Grid>
  <TextBlock Grid.Row="3" Text="详细安排 / 住宿 / 注意事项" Margin="0,10,0,6"/>
  <TextBox Grid.Row="4" Text="{Binding Details, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="9" MinHeight="100" AutomationProperties.Name="日程详情"/>
 </Grid>
</UserControl>
